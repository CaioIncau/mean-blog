<h2>Posts</h2>
	<table class="table">
  <tr>
    <th>Titulo</th>
    <th>Texto</th>
  <tr ng-repeat="post in posts ">
    <td>
      <a ng-href="#/posts/{{contato._id}}">{{post.title}}</a>
    </td>
    <td>{{post.title}}</td>
    <td>{{post.text}}</td>
</tr>
</table>